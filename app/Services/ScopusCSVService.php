<?php

namespace App\Services;

class ScopusCSVService
{
    public function process($path)
    {
        // Lógica para processar o arquivo CSV da Scopus
        // Exemplo: var_dump(file(storage_path('app/'.$path)));
    }
}